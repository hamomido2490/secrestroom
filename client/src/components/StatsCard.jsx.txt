function StatsCard({ number, label }) {
  return (
    <div className="stats-card">
      <div className="stats-number gradient-text">{number}</div>
      <div className="stats-label">{label}</div>
    </div>
  )
}

export default StatsCard