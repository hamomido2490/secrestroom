import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import App from '../src/App.jsx'

describe('App', () => {
  it('renders welcome title', () => {
    render(<App />)
    expect(screen.getByText(/غرفة الأسرار/i)).toBeInTheDocument()
  })
})